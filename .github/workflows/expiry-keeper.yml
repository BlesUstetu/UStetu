name: UStetu Expiry Keeper (Legacy Disabled)

on:
  workflow_dispatch:

jobs:
  legacy-disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Protocol note
        run: |
          echo "Legacy keeper disabled."
          echo "UStetu V1 expiry and auto-release functions are permissionless."
          echo "No off-chain keeper is required for protocol correctness."
